# Flowclass Open-Source Agent Rules

- **Git Push Guard**: This is a public repository. Never push to any remote branch (`git push`) unless the user has explicitly reviewed the code changes and granted permission to do so.
